"""URL configuration for Access module."""
from django.urls import path

app_name = 'access'

urlpatterns = [
    # API endpoints will be added here
    # Example: path('memberships/', views.MembershipListView.as_view(), name='membership-list'),
]
