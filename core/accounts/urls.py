"""URL configuration for Accounts module."""
from django.urls import path

app_name = 'accounts'

urlpatterns = [
    # API endpoints will be added here
    # Example: path('profile/', views.ProfileView.as_view(), name='profile'),
]
